OCAMLMAKEFILE = ../OCamlMakefile
COMMON = ../common/
SOURCES = bound.ml dashDot.ml $(COMMON)integer.ml test.ml
RESULT = main
ANNOTATE = true
DOC_FILES = bound.ml dashDot.ml
PACKS = str

all: dnc

include $(OCAMLMAKEFILE)
